@model IEnumerable<darlenyWebApp.Models.WebApp.UnitMeasure>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Description)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Acron)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Acron)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UnitMeasureID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UnitMeasureID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UnitMeasureID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
